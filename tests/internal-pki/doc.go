// Package internalpki owns the repository's external-boundary internal PKI
// integration harness. It deliberately has no dependency on either product
// module so that the tests can only observe shipped process behavior.
package internalpki
