# 需求：内部 PKI 与隧道 mTLS 剩余任务承接

日期：2026-08-02

## 一屏摘要

- 目标：以 git HEAD（`1ca08656`）为基线完成旧工作流剩余 5 个任务，交付完整的内部 PKI 自动轮转与 relay 隧道双向 mTLS（R1–R6）
- 使用者：继续该研发的工作流执行者，以及使用面板与 join/upgrade 流程的部署管理员（R2、R4）
- 范围：agent 侧隧道凭据、relay 数据面 mTLS、面板 PKI 界面、升级运维文档、端到端验证（R2–R6）
- 关键规则：旧需求 R1–R13 的行为约束全部继承；控制协议保持 HTTP + `X-Agent-Token` 非 mTLS，mTLS 仅覆盖 relay 数据面（R7）
- 成功标准：5 个任务的 outcomes 达成且各自 verify 命令通过，旧计划 `delivery_verification` 全量命令通过（R8）
- 不做事项：不重做/不复审已完成 6 个任务、不补 `go test -race`、不修复旧工作流 state 文件（R9–R11）

## 完整需求

### R1 基线与承接方式

新工作流以仓库 git HEAD（提交 `1ca08656`，即旧工作流 `pki-production-runtime` A1 的 checkpoint）为实现基线，直接承接旧工作流的探索、技术方案与执行计划，不重新进行需求探索或方案设计。旧工作流已完成并通过审查的 5 个任务（`pki-storage`、`pki-enrollment-core`、`pki-backup-lease`、`pki-lifecycle`、`control-integration`）以及已完成执行但未回写状态的 `pki-production-runtime`（`done_with_concerns`，全量测试/integration/vet 通过）均视为既有事实与可用基础。

验收：

- 新工作流的首个任务从 `agent-credentials` 开始，基线提交为 `1ca08656` 或其后续提交。
- 实现过程中可引用旧工作流的 `01-exploration.md`、`02-technical-solution.md`、`03-execution-plan.md` 作为设计依据，无需重新产出同类文档。

### R2 agent 侧隧道凭据与登记流程（agent-credentials）

实现 agent 侧 tunnel key/CSR、跨平台 generation 激活、安全快照持久化和嵌入式独立身份，并更新 join/upgrade 凭据流程。remote/embedded agent 的 tunnel 私钥只在所属 data dir 生成并以受限权限保存，控制面只收到 CSR；immutable generation 经完整验证后原子替换 active pointer（Unix rename / Windows ReplaceFileW/MoveFileExW），崩溃只留下旧或新完整状态；agent 原子持久化 PKI domain、(epoch, revision)、trust/revocation snapshot 并拒绝 downgrade，higher epoch/revision 0 完整快照可恢复；join/upgrade 使用一次性 register token 取得 tunnel credential，同时保留 `NRE_AGENT_TOKEN` 作为现有控制协议凭据并以 0600 保存；本地嵌入式 agent 保留进程内 control bridge 并使用独立 tunnel credential store。

验收：

- `cd go-agent && go test -short ./internal/modules/pki ./embedded` 通过。
- `sh scripts/join-agent.test.sh` 通过。
- 控制面在任何路径下都拿不到 agent 的 tunnel 私钥，只能收到 CSR。

### R3 relay 数据面双向 mTLS（relay-mtls）

将 relay TLS/TCP 与 QUIC 数据隧道改为严格双向 mTLS，transport fallback 复用同一身份、撤销和 generation 规则。relay client/server 同时验证 CA、时间、EKU、DNS/IP、PKI-domain/agent/listener URI identity、cached revocation 和 CA generation；无证书、非受信、过期、撤销、错误用途或身份不匹配全部拒绝；pin-only、单向 TLS 和非认证 fallback 从内部 relay 删除；QUIC 到 TLS/TCP fallback 只改变 transport，不改变 mTLS verifier 或 credential generation；在线 revoke/CA security update 在五秒内关闭相关 session；控制面短时不可用时 relay 继续使用最后可信 snapshot，重连后安全更新优先。

验收：

- `cd go-agent && go test -short ./internal/modules/relay ./internal/control ./internal/app -run 'Test(Relay.*MTLS|TunnelIdentity|SecuritySnapshot|ControlProtocolUnchanged)'` 通过。
- 攻击矩阵（无证书/非受信/过期/撤销/错误 EKU/身份不匹配）的连接均被拒绝，且有反向测试证明 heartbeat/revision/task 控制请求不被要求客户端证书。

### R4 面板内部 PKI 界面（panel-pki）

提供与公网证书分域的内部 PKI 生命周期、告警、审计、处置、登记和受保护备份界面。公网证书与内部 PKI 明确分区；内部页展示 identity、owner、purpose、chain/generation、serial/fingerprint、有效期、next action、rotation phase 和错误；管理员可一次查看 register token，并通过明确确认执行 revoke、force endpoint rotate、normal/emergency CA rotate、迁移 activation；derived warning/critical 和 audit 查询覆盖签发、续签、轮转、撤销、拒绝、备份/恢复的对象、来源、结果与原因；加密备份导出/导入口令只驻留单次请求内，UI 不再宣称备份未加密或持久化口令。

验收：

- `cd panel/frontend && npm test` 通过，页面行为测试覆盖 operation polling、错误恢复、高风险确认与内部/公网边界。
- 面板中公网证书与内部 PKI 在导航和表单上可明确区分，内部 PKI 的处置动作均需显式确认。

### R5 升级与运维文档（upgrade-docs）

固化 tunnel-only mTLS 的部署、升级、备份迁移与安全运维契约，不引入第二控制入口。部署材料继续只暴露现有 panel/control listener，并说明 mTLS 只覆盖 relay TLS/TCP/QUIC；升级手册说明维护窗口、bound enrollment、稳定 agent 关联、embedded tunnel identity 和旧 relay authentication 删除点；备份/计划迁移/force activation 文档说明口令、PKI epoch、`NRE_MASTER_URL` 地址更新、单活边界与离线撤销延迟；公网 ACME、面板/API 用户和公开业务客户端不被描述为内部 mTLS consumer。

验收：

- `docker compose --env-file .env.example config` 通过。
- README、`.env.example`、`docker-compose.yaml`、`Dockerfile`、`docs-site/**` 中的相关描述与实际行为一致，且不含第二控制入口。

### R6 端到端验证与 CI 契约（pki-e2e）

建立多进程内部 PKI/relay mTLS 端到端夹具和 CI 契约，机械证明正常、失败、攻击、恢复与迁移行为。隔离夹具启动控制面、远程 agent、embedded agent 和 relay listener，并以可注入时钟验证三分之一续签、换钥和 CA 最长 30 天换代；攻击矩阵覆盖无证书、非受信、过期、撤销、错误 EKU/agent/listener/PKI domain 及安全快照 downgrade，且 control protocol 保持 token；处置/恢复矩阵覆盖在线五秒撤销、离线快照可用与重连收敛、ack timeout blocked、force/emergency rotate、crash recovery 和跨平台 active pointer；加密备份篡改/错误口令失败不改变状态，sanitized snapshot 无 enrollment token；跨目录/地址恢复保持 PKI/agent/关联并验证 epoch fencing 与单活；CI 区分快速与 integration 层，并保留公网 Pebble ACME 与容器构建回归。

验收：

- `sh scripts/test-internal-pki-e2e.sh` 通过。
- CI 配置中快速层与 integration 层分离，且既有公网 Pebble ACME 与容器构建回归保留。

### R7 既有行为约束继承

旧需求文档 R1–R13 的全部行为约束继续有效，本需求不放宽其中任何一项。特别强调：控制链路（heartbeat/revision/task/registration）保持 HTTP + `X-Agent-Token`，不要求客户端证书、不新增监听器或端口；mTLS 仅覆盖 relay TLS/TCP/QUIC 数据面；公网 ACME 证书的申请、续签与分发行为不受内部 PKI 影响。

验收：

- 任一任务实现后，既有控制协议反向测试（不要求客户端证书、URL 与认证头不变）通过。
- 公网 ACME 相关既有测试不出现回归。

### R8 整体验收

全部 5 个任务完成后，旧执行计划的 `delivery_verification` 全量命令通过：backend/agent 快速层与全量层、frontend 行为与构建、backend/agent integration、内部 PKI e2e、`docker compose config`、容器镜像构建。

验收：

- `cd panel/backend-go && go test -short ./... && go test ./...`、`cd go-agent && go test -short ./...`、`cd panel/frontend && npm test && npm run build`、`docker compose --env-file .env.example config`、`docker build -t nginx-reverse-emby .` 均通过。

## 不做事项

### R9 不重做、不复审已完成任务

旧工作流已完成的 6 个任务（含 `pki-production-runtime` A1）不在本需求范围内：不重新实现、不补审查收口、不因旧 state 文件未回写而重复执行。

验收：

- 新工作流的任务列表中不出现上述 6 个任务的实现或复审条目。

### R10 不补 go test -race 验证

`pki-production-runtime` A1 因当前 Windows 环境无 CGO 而未执行 `go test -race`；该现状已被接受（已有 integration 层、Linux 交叉编译、高风险用例重复执行与三路独立复审补强），本需求不安排补验。

验收：

- 交付验证清单中不包含 `go test -race` 相关条目。

### R11 不修复旧工作流状态文件

旧工作流目录（`docs/sakullla-workflow/2026-07-31-内部证书自动轮转与隧道双向mtls/`）的 state 与仓库进度脱节问题，通过新工作流从 HEAD 重开规避；本需求不包含对旧 state 文件的修复或迁移。

验收：

- 交付物中不包含对旧工作流 `00-state.yaml` 的修改。

## 来源

- `docs/requirements/2026-07-31-内部证书自动轮转与隧道双向mtls.md`：R1–R13 原始行为约束，本需求 R7 整体继承。
- `docs/sakullla-workflow/2026-07-31-内部证书自动轮转与隧道双向mtls/03-execution-plan.md`：R2–R6 的任务目标、outcomes 与 verify 命令，以及 R8 的 `delivery_verification` 清单。
- `docs/sakullla-workflow/2026-07-31-内部证书自动轮转与隧道双向mtls/00-state.yaml` 与 `task-runs/pki-production-runtime-run.md`：R1 的已完成任务清单、基线提交与 R10 的 race 现状依据。
